---
description: Reloads Superpowers configuration by re-reading Rules, Workflows, and Skills from disk.
---

# Superpowers Reload

## 0. KOS Check & Auto-healing
Before reading the directories below, you MUST use your file-system tools (like `list_dir` or `view_file`) to check if `artifacts/knowledge/tech_stack.md` exists. 
If it is missing, you MUST automatically create the `artifacts/knowledge/` directory and its files using your native file-writing tools (Gate 0 Initialization). Do not proceed to reload until KOS is verified or recreated.

## 1. Reload Configuration
Read these directories from disk and summarize what you loaded:
- `.agent/rules/`
- `.agent/workflows/`
- `.agent/skills/` (list skill names + descriptions)

Then confirm you will follow the latest versions in this session.
Stop after confirming.
